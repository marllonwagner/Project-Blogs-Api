const auth = require('./Auth');
const userValidations = require('./UserValidations');
const categoryValidations = require('./CategoryValidations');
const blogPostValidations = require('./BlogPostValidations');

module.exports = {
  auth,
  userValidations,
  categoryValidations,
  blogPostValidations,
};